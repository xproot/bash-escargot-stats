# bash-escargot-stats
Bash scripts that checks Escargot's status and stats (currently online users only)

Needs
* **Bash** (both scripts)
* Netcat (status only)
* grep  (status only)
* curl (stats only)

